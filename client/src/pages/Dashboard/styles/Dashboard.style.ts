import { makeStyles } from "@material-ui/core";

export const useDashboardStyles = makeStyles((theme) => ({
	root: {
		display: "flex",
		flexGrow: 1
	},
	gridContainer: {
		margin: 0,
		width: "100%"
	}
}));
